#include <stdbool.h>

bool hasAlternatingBits(int n) {
    // Shifting n right by 1 and XORing it creates an all-ones mask if bits alternate
    unsigned int x = n ^ (n >> 1);
    
    // Check if x is of the form 111...11 (all bits set to 1)
    return (x & (x + 1)) == 0;
}
